import * as React from 'react';

import { Layout } from '../layout/dashboard/layout';


const Settings = () => {
    return (
        <Layout>
            Settings
        </Layout>
    );
}

export default Settings;
